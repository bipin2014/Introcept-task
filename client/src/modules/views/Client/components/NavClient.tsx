import React, { useEffect, useState } from 'react';



const NavClient = () => {

    return (
        <nav className={'pa-md top-nav flex items-center justify-between'}>
            <div className="nav-header title-sm flex-1 text-center title-sm bold text-primary">{'Client Management System'}</div>
        </nav>
    );
}

export default NavClient;
